# Plugin API repository instructions

The API repository owns only the language-neutral contract and its fixtures.
Run `./tests/contract/run.sh` for focused verification. Do not add provider
semantics, PHP SDK implementation, or core runtime code here.
