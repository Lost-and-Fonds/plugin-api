# Contract fixtures

These small JSON values are language-neutral examples for SDK conformance
tests. They use the RPC v1 JSON representation described in the repository
README. Future SDKs can replay them without depending on PHP classes.

The set includes representative Input and Broadcast lifecycle messages, a
capability request/response, and a typed retryable failure.
